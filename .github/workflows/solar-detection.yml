name: Solar Detection Cron

on:
  # Scheduled trigger deaktiviert — Server-Cron auf Hetzner läuft
  # zuverlässiger. Workflow bleibt als Manual-Backup.
  workflow_dispatch:

jobs:
  solar-detection:
    runs-on: ubuntu-latest
    steps:
      - name: Run OSM Solar Detection
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/cron/solar-detection")

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n -1)

          echo "HTTP Status: $HTTP_CODE"
          echo "Response: $BODY"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "Solar detection cron failed with status $HTTP_CODE"
            exit 1
          fi
